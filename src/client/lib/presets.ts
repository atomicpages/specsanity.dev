export const presets = {
  minimal: {
    extends: ["minimal"],
  },
  recommended: {
    extends: ["recommended"],
  },
  "recommended-strict": {
    extends: ["recommended-strict"],
  },
} as const;
